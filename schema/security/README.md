# Security

Security objects and RLS policies are implemented in `schema/migrations/0003_security.sql`, with data-driven security configuration in `0006_security_config.sql` and agent write-path gating in `0007_update_node_properties.sql`.
