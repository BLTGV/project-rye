interface:
  display_name: "Rye Tabular Intake"
  short_description: "Extract, map, group, and stage CSV/XLSX sources for Rye-tracked loads"
  default_prompt: "Inspect tabular source files, emit source-set lineage, map or group fields, and stage import tracking records for Rye."
